# Captured host configs

Deployment configs that live on hosts (not otherwise in the repo), captured for
reproducibility. **Secrets are redacted** — real values live in `/root/.*-pw` +
the php-fpm pool env on each host, never here. Per-host subdirs.
