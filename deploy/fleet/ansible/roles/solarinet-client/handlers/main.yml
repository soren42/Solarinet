# solarinet-client handlers.
---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart solarinet-client
  ansible.builtin.systemd:
    name: solarinet-client.service
    state: restarted
